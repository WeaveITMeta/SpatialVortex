# SurfaceLight — Surface-emitting light (child of BasePart)
# Bevy: Custom surface light component

[light]
color = [255, 255, 255]
brightness = 1.0
range = 16.0
angle = 0.0
shadows = false
enabled = true
face = "Front"

[metadata]
class_name = "SurfaceLight"
archivable = true
