# LuauScript — Server-side Luau script (Roblox-compatible)
# mLuau VM: Compiled and executed via mlua crate with luau feature

[script]
source = ""
enabled = true
run_context = "Server"

[metadata]
class_name = "LuauScript"
archivable = true
