# LuauLocalScript — Client-side Luau script (Roblox-compatible)
# mLuau VM: Runs in client context

[script]
source = ""
enabled = true
run_context = "Client"

[metadata]
class_name = "LuauLocalScript"
archivable = true
