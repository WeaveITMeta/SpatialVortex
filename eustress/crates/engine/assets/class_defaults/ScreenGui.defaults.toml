# ScreenGui — Fullscreen 2D overlay root container
# Bevy: Node (100% width/height, absolute position)

[gui]
enabled = true
display_order = 0
ignore_gui_inset = false
reset_on_spawn = true
z_index_behavior = "Sibling"

[metadata]
class_name = "ScreenGui"
archivable = true
