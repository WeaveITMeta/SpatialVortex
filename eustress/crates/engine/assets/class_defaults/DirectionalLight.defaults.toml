# DirectionalLight — Scene-wide directional light (sun/moon)
# Bevy: DirectionalLight component

[light]
color = [255, 255, 255]
brightness = 1.0
shadows = true
enabled = true

[metadata]
class_name = "DirectionalLight"
archivable = true
