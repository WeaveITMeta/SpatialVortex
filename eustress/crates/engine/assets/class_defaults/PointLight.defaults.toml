# PointLight — Omni-directional light source (child of BasePart or Attachment)
# Bevy: PointLight component

[light]
color = [255, 255, 255]
brightness = 1.0
range = 16.0
shadows = false
enabled = true

[metadata]
class_name = "PointLight"
archivable = true
