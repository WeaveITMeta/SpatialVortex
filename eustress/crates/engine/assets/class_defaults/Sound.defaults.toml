# Sound — Audio playback (child of BasePart or Workspace)
# Bevy: AudioSource / SpatialAudioSink

[sound]
sound_id = ""
volume = 0.5
looped = false
playing = false
playback_speed = 1.0
rolloff_max_distance = 10000.0
rolloff_min_distance = 10.0
rolloff_mode = "InverseTapered"
time_position = 0.0

[metadata]
class_name = "Sound"
archivable = true
