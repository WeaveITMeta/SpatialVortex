# Clouds — Volumetric cloud system (child of Lighting service)
# Bevy: Custom volumetric cloud shader

[clouds]
enabled = true
cover = 0.5
density = 0.7
color = [255, 255, 255]

[metadata]
class_name = "Clouds"
archivable = true
